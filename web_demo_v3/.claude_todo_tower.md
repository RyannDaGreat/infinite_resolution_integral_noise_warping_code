# Platforming Tower TODO

- [x] Read all existing files (physics.js, scene.js, shaders.js, geometry.js, main.js, renderer.js, ui.js)
- [x] Add tower generation to physics.js:
  - [x] Add tower constants (TOWER_CENTER_X/Z, TOWER_TARGET_HEIGHT, friction values)
  - [x] Write seeded PRNG (`makeTowerRng`) and path generation algorithm (`generateTowerPath`)
  - [x] Write `_createPlatformTower()` method with platforms, ramps, and flag
  - [x] Add tower data to `getSceneData()` via `extractBody` helper
  - [x] Call `_createPlatformTower()` in `init()`
  - [x] Store bodies in `this.towerPlatforms`, `this.towerRamps`, `this.towerFlag`
- [x] Add tower rendering to scene.js:
  - [x] Add tower instances in `buildInstances()` (platforms, ramps, flag)
  - [x] MAX_INSTANCES already 2048 — sufficient
- [x] Fix shader: change isDomino from catch-all to explicit color match (0.92, 0.90, 0.85)
- [x] Verify renderer draws tower instances correctly (flat boxVB for all tower elements)
